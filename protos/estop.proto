syntax = "proto3";

import "protos/header.proto";

message stopRequest {
    header header = 1;
    google.protobuf.Timestamp time = 2;
}

service stopService {
    rpc stop(stopRequest) returns (bool) {}
}